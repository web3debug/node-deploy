#!/usr/bin/expect
set datadir [lindex $argv 0]
# 10 num at least wanted
set wallet_password [lindex $argv 1]

set timeout 5
spawn ./bin/geth bls account new --datadir $datadir
expect "*assword:*"
send "$wallet_password\r"
expect "*assword:*"
send "$wallet_password\r"
sleep 1
expect EOF